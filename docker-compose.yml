version: '3.8'

services:
  goal-manager:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    container_name: goal-manager-app
    restart: unless-stopped
    environment:
      - NODE_ENV=production
